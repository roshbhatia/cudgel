version: '3'

vars:
  PROJECT_NAME: cudgel

tasks:
  default:
    desc: Show available tasks
    cmds:
      - task --list

  # Development
  build:
    desc: Build the project (release mode)
    cmds:
      - cargo build --release

  test:
    desc: Run all tests
    cmds:
      - cargo test

  fmt:
    desc: Format code
    cmds:
      - cargo fmt

  clippy:
    desc: Run linter
    cmds:
      - cargo clippy --all-targets -- -D warnings

  check:
    desc: Quick check (no codegen)
    cmds:
      - cargo check

  # Installation
  install:
    desc: Install cudgel globally
    cmds:
      - cargo install --path .

  deps:
    desc: Install all dependencies (models, database, schema) via cudgel deps
    cmds:
      - cargo run --release -- deps

  deps-check:
    desc: Check dependency status without installing
    cmds:
      - cargo run --release -- deps --check

  teardown:
    desc: Complete teardown - removes all data and uninstalls cudgel
    prompt: This will stop PostgreSQL, remove all data, and uninstall cudgel. Continue?
    cmds:
      - echo "Cleaning all dependencies..."
      - cargo run --release -- deps --clean-all || true
      - echo "Uninstalling cudgel binary..."
      - rm ~/.cargo/bin/cudgel 2>/dev/null || true
      - echo "Cleaning build artifacts..."
      - cargo clean
      - echo "✓ Teardown complete - system is clean"

  run:
    desc: "Run cudgel (usage: task run -- index .)"
    cmds:
      - cargo run --release -- {{.CLI_ARGS}}

  # Setup
  setup:
    desc: Build, install, and setup all dependencies via cudgel deps
    cmds:
      - echo "Building cudgel..."
      - cargo build --release
      - echo "Installing cudgel..."
      - cargo install --path .
      - echo "Setting up dependencies..."
      - cudgel deps
      - echo "✓ Setup complete!"
      - echo ""
      - echo "PostgreSQL is running on localhost:45678"
      - echo "Data directory:" $HOME/.local/share/cudgel/postgres
      - echo "Models directory:" $HOME/.local/share/cudgel/models

  db-start:
    desc: Start native PostgreSQL
    cmds:
      - ./scripts/start-postgres.sh

  db-stop:
    desc: Stop PostgreSQL
    cmds:
      - ./scripts/stop-postgres.sh

  db-status:
    desc: Check PostgreSQL status
    cmds:
      - |
        PGPORT=${CUDGEL_POSTGRES_PORT:-45678}
        pg_isready -p $PGPORT -h localhost && echo "PostgreSQL is running on port $PGPORT" || echo "PostgreSQL is not running"

  health:
    desc: Check development environment health
    cmds:
      - |
        echo "Checking development environment..."
        echo ""
        echo "Use 'cudgel deps --check' for detailed dependency status"
        echo "Use 'cudgel deps --check --verbose' for diagnostic information"
        echo ""
        PASS="\033[0;32m✓\033[0m"
        FAIL="\033[0;31m✗\033[0m"
        WARN="\033[0;33m⚠\033[0m"

        # Rust toolchain
        if command -v rustc > /dev/null 2>&1; then
          echo -e "$PASS Rust toolchain ($(rustc --version | cut -d' ' -f2))"
        else
          echo -e "$FAIL Rust toolchain - not found"
        fi

        # Cudgel binary
        if command -v cudgel > /dev/null 2>&1; then
          echo -e "$PASS cudgel binary installed"
        else
          echo -e "$WARN cudgel binary - not installed (run 'task install')"
        fi

        # Ollama (optional, for US3)
        if command -v ollama > /dev/null 2>&1; then
          echo -e "$PASS ollama (optional, for knowledge generation)"
        else
          echo -e "$WARN ollama - not installed (optional)"
        fi
